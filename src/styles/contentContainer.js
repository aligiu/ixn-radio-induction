import { StyleSheet } from "react-native";

export const contentContainerStyles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 8,
    paddingLeft: 16,
    paddingRight: 16,
    // borderColor: "grey",
    // borderWidth: 1,
  },
});
