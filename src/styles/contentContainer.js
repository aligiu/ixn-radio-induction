import { StyleSheet } from "react-native";

export const contentContainerStyles = StyleSheet.create({
  container: {
    flex: 1,
    margin: 10,
    borderColor: "grey",
    borderWidth: 1,
  },
});
