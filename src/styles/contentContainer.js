import { StyleSheet } from "react-native";

export const contentContainerStyles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 8,
    marginLeft: 16,
    marginRight: 16,
    // borderColor: "grey",
    // borderWidth: 1,
  },
});
